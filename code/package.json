{
  "name": "cfda-trec-rag-2026-final-pipeline",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:ts": "tsx --test tests/*.test.ts",
    "test:smoke": "tsx --test tests/bounded_rag_smoke.test.ts",
    "test:python": "python3 -m unittest discover -s tools/tests -p 'test_*.py'",
    "check": "npm run typecheck && npm run test:ts && npm run test:python",
    "run:retrieval": "bash scripts/run_retrieval_policy.sh",
    "run:rag": "bash scripts/run_final_rag.sh"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1",
    "tsx": "^4.20.6",
    "typebox": "^1.3.9"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  },
  "overrides": {
    "sharp": "0.35.0"
  }
}
